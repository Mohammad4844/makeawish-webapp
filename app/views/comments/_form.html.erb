<%= form_with(url: new_post_comment_path(post_id), class: "input-group ") do |form| %>

    <%= form.hidden_field :post_id, value: post_id %>
    <%= form.text_field :content, class: "form-control ", placeholder: "Add a Comment" %>
    <%= form.submit "+", class: "btn btn-success" %>
  
<% end %>